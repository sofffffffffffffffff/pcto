#include<stdio.h>

int main()
{
    //VARIABILI//
    int a; //muneri interi
    float b = 2.5;// numeri con massimo 6 numeri dopo la virgola
    double c = 2.6;// numeri con massimo 15 numeri dopo la virgola, sono più precisi
    a = 2;
    printf("a:%d\n", a);
    printf("b:%f\n", b);
    printf("c:%lf\n", c);
}